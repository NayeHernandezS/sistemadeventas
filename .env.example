# Copia este archivo a .env en la raiz del proyecto y completa tus valores.
# Spring Boot lo carga automaticamente (no subas .env a git).
# Alternativa: export DB_URL=... DB_USER=... DB_PASSWORD=... en la terminal o en el IDE.

DB_URL=jdbc:mysql://localhost:3306/java_curso?serverTimezone=America/Mexico_City
DB_USER=root
DB_PASSWORD=16Crepusculo
